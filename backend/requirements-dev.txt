# Deps de desenvolvimento/teste — nao vao pra producao.
# Instalar com: pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
# httpx ja esta em requirements.txt (cliente do Nominatim).
# FastAPI/Starlette TestClient usa httpx internamente.

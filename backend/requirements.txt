fastapi==0.115.0
uvicorn[standard]==0.30.6
# uvloop e Linux/macOS apenas — marker pula instalacao no Windows.
# No container (Linux) e instalado normalmente e usado via --loop uvloop.
uvloop==0.20.0; sys_platform != "win32"
SQLAlchemy==2.0.49
psycopg[binary]==3.2.13
alembic==1.13.3
pydantic==2.13.4
pydantic-settings==2.14.1
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.12
structlog==24.4.0
email-validator==2.2.0
httpx==0.27.2
# Extrai arquivos individuais de ZIPs remotos via HTTP Range — usado pra
# fotos de candidatos TSE (zips estaduais de 2GB+, baixamos so 45KB por foto).
remotezip==0.12.3
# Geracao de dossie PDF do candidato (foto + dados + votos por municipio/zona).
reportlab==4.2.5
# QR code na capa do dossie + dependencia de imagem (reportlab usa pra embed PNG).
qrcode==7.4.2
pillow==10.4.0
# Rate limiting (anti-DoS) — limites por IP em endpoints sensiveis
# como /photo, /sync, /login. In-memory storage (1 worker = sem necessidade
# de Redis). Suporta X-Forwarded-For pra ler o IP real atras do nginx.
slowapi==0.1.9
# Brotli ASGI middleware — comprime JSON/text antes do nginx servir.
# Brotli e 20-25% menor que gzip pra payloads JSON. Aceita "br" no
# Accept-Encoding. Browser cai pra gzip se nao suportar.
brotli-asgi==1.4.0

# IBGE shapefile reader (POC dados censitários) — pure-Python, sem GDAL.
pyshp==2.3.1

# Serializacao JSON em Rust — 5-10x mais rapido que json.dumps pra payloads
# grandes (GeoJSON do censo, rankings TSE). Usado como default_response_class.
orjson==3.10.12

# Leitura de planilhas .xlsx do governo (INEP/IDEB e outras fontes que só
# publicam Excel) nos scripts de ingestão socioeconômica.
openpyxl==3.1.5
